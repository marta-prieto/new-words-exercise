/* function haveSameLetters(casasa, casaca) {

  const words = {

    getNewWords: [
      'casasa',
      'casaca'
    ],
    changeWord: ''

    
  };
  console.log(words.getNewWords[1])

   */
/*   const checkNewWord = words.map(name => name.includes('a'));

  console.log(checkNewWord); */

  /* 
    const newWord = '';
    const isWord = newWord === 'casasa'; //true
    const secondWord = newWord === 'casaca'; //false 
  
    console.log(haveSameLetter(''));
  
    if (isWord === newWord) {
      return true;
    } else (secondWord !== newWord)
    return false; */


  /* console.log(haveSameLetters('casaca', 'sacasa'))

  Y nos devolverá false

  O
  haveSameLetters('casa', 'saca')
  Y nos devolverá true */
}

/* 
function haveSameLetters (casasa, casaca) {

  const words = ['casasa', 'casaca'];
const checkNewWord = words.map( name => name.includes('sa') );

console.log(checkNewWord);


  const newWord = '';
  const isWord = newWord === 'casasa'; //true
  const secondWord = newWord === 'casaca'; //false 

  console.log( haveSameLetter ( '' ) );
 
  if (isWord === newWord) {
    return true;
  } else (secondWord !== newWord)
  return false;
} */

const a = 8;
console.log(a);

/*
function getWords(casasa, casaca) {
  const newWord = casasa;

  if (newWord === casasa) {
    return true;
  } else if (newWord !== casasa) {
    return false;
  } else (newWord === casaca)
  return false;
}
  */

/*  const words = ['casasa', 'casaca'];
 const checkNewWord = words.map( name => name.includes('sa') );

 console.log(checkNewWord); */